//to stop the character from moving when the object being pushed collides into another object

if(dpad_left_obj.press == 1)
{
dpad_left_obj.press = 0;
}

else if(dpad_right_obj.press == 1)
{
dpad_right_obj.press = 0;
}

else if(dpad_up_obj.press == 1)
{
dpad_up_obj.press = 0;
}

else if(dpad_down_obj.press == 1)
{
dpad_down_obj.press = 0;
}
