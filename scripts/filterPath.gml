/*
filterPath(id, Node): Boolean
Filters the path in the pathfinding process.  
Must return true if argument[1] node is "walkable" or false otherwise.
argument[0] is the instance calling the getShortestPath script.

Useful for blocks that dynamically appear (For static ones, just don't connect the nodes)
*/

return(true);
