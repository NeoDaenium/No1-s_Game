/*
getNeighbours(NodeID): ds_map{Node, Distance}
Returns a map containing the neighbours of the given node and the distances between them (The weight of the edge)
*/

return(__Nodes[argument[0], 1]);
